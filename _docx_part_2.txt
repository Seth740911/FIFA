re reliable). Extracted all cache logic into an independent _startBackgroundCache() function called from the MANIFEST_PARSED handler.Bug 6: Mobile direct-fetch FIFA CDN returns 403 [CRITICAL (mobile)]Discovery via /api/diag: Mobile diagnostic showed bg_cache_failed: "status 403".FIFA CDN URLs with expired or reused signature tokens reject direct requests from mobile browsers.The _startBackgroundCache() function was attempting to fetch m3u8 and .ts segments directly from FIFA CDN.影响: ROOT CAUSE for mobile. Even after fixing the event trigger (Bug 5), the cache download failed because FIFA CDN rejected the requests with 403.修复: _startBackgroundCache() now fetches m3u8 through the /proxy/ endpoint (server-side proxy) instead of direct fetch. It passes viaProxy: true to _cacheVideoSegments so all .ts segment downloads also go through the proxy. This ensures FIFA CDN sees the request coming from the server's IP, not the mobile browser.5. Syntax Error IncidentAfter fixing Bug 3 (replacing synchronous XHR with async fetch), orphaned try/catch code residues remained in the file at lines 2599-2617. This caused a JavaScript syntax error: "missing ) after argument list", which resulted in a completely blank white screen for all users.The fix was to remove the orphaned code block. This incident highlights the importance of verifying the complete file after refactoring, not just the modified lines.6. Visual Cache Status IndicatorTo help users and developers understand cache state, we added a visual indicator below the video player:StateDisplayMeaningFull cache📱 本地缓存 33/33 (100%)All segments cached, plays from localCaching⬇ 缓存中 12/33 (36%)Download in progressNetwork only🌐 网络流播放No cache available, streamingPartial + network📱 缓存 20/33 (60%) + 🌐 补缺Partial cache + fetching missing segmentsImplementation: tlCacheBar (3px progress bar) + tlCacheStatus (text) driven by _updateCacheStatus() called at all key lifecycle points.7. Fix SummaryBug #SeverityRoot CauseFix Strategy1CRITICALDeletes incomplete cacheKeep incomplete cache, play with partial + fresh URLs2HIGHExpired CDN signaturesFetch fresh m3u8 for missing segment URLs3CRITICAL (mobile)Synchronous XHRReplace with async fetch()4MEDIUMNo quota handlingAuto-evict oldest caches on quota error5CRITICAL (mobile)MANIFEST_LOADED unreliableSwitch to MANIFEST_PARSED trigger6CRITICAL (mobile)FIFA CDN 403 on mobileRoute all cache downloads through /proxy/8. Verification Results8.1 PC Verification (Chrome)Open match page → Video streams from networkBackground cache downloads all 33 segments → cache_completeNavigate away and return → Video plays from local cache (0 network requests)Cache status shows: 📱 本地缓存 33/33 (100%)8.2 Mobile Verification (Honor RNA-AN00, Android 14)Open match page → Video streams via proxy fallbackBackground cache downloads all 34 segments via /proxy/ → cache_completeReturn to same match → Video plays from local cacheUser confirmed: "有啦，缓存中" (It's here, caching in progress)9. Key LessonsMobile is not ju