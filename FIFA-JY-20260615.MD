# FIFA 2026 项目改进建议（终版，共57条）

> 第5版 · 融合5轮审查 · 2026-06-15

---

## 🔴 P0 — 安全隐患（建议48小时内处理）

| # | 建议 | 具体方案 | 工作量 |
|---|------|---------|--------|
| 1 | /proxy/ + /video/ 加域名白名单 | 只允许 `webcurl.7m.com`、`api.fifa.com`、`uplynk.com`；`/video/http://...` 同样可以代理任意URL，要一起加 | 1h15min |
| 2 | /video/ 限制文件扩展名 | 当前 `D:\迅雷下载` 下任何文件（含 .txt、.torrent 等）均可通过 URL 访问。应只允许 `.mp4`、`.m3u8`、`.ts`、`.key`，其他返回 403 | 15min |
| 3 | /api/stats 加鉴权 | 加 Token 校验或限制只返回聚合数据（不返回IP列表） | 1h |
| 4 | /api/diag 加限制 | 限制 POST body 大小（≤1MB）、加速率限制（同一IP每分钟最多5次） | 1h |
| 5 | /api/sync 加 cooldown | 虽有 `_sync_running` 防并发，但用户可快速连点触发多次 `fetch_scores.py`。建议记录上次同步完成时间，60秒内直接返回当前数据 | 15min |
| 6 | CORS 策略收紧 | 从 `*` 改为 `http://192.168.0.10:8086`，或用动态校验 | 30min |

**P0 总计：约 4.75h**

---

## 🟠 P1 — 性能瓶颈 + 数据安全（建议本周处理）

### 性能瓶颈（按投入产出比排序）

| # | 建议 | 具体方案 | 工作量 |
|---|------|---------|--------|
| 7 | 删除 wc-photos 重复目录 | 删除全部全名目录（ALGERIA/、AUSTRALIA/ 等），只保留短代码目录（ALG/、AUS/ 等）；或用 symlink 代替复制。节省 ~550MB | 30min |
| 8 | HTTP 压缩（gzip） | 用 Python 标准库 gzip 压缩响应；`wc-player-7m.js` 4MB 压缩后约800KB；5个数据JS加起来压缩能省70%+流量 | 1h |
| 9 | 心跳机制改内存缓存 | 当前每30秒写一次 `online_users.json`（N个在线用户 = 每30秒 2N 次磁盘I/O）。改为内存字典，每5分钟才持久化一次 | 1h |
| 10 | wc-player-7m.js 改为按需加载 | 点击球员时才动态 `import()` 或 AJAX 请求；或拆分成26个字母文件只加载需要的。该文件仅7行但每行~570KB，浏览器解析时阻塞主线程数秒 | 4h |
| 11 | 照片无缩略图 | roster 页卡片加载的是 `wc-photos/` 全尺寸原图（单张可能几百KB），一页展示几十张就是十几MB。应预生成缩略图目录 `wc-photos-thumb/`，roster 页用缩略图，点击详情才加载原图 | 3h |
| 12 | 大文件改为流式传输 | `/video/` 和 `/apk/` 端点：`f.read(8192)` 分块写入 `self.wfile`，不用 `f.read()` | 2h |
| 13 | 代理改为流式传输 | `_proxy_remote` 函数：`requests.get(stream=True)` + 分块转发，不全量缓存 | 2h |
| 14 | Stats API 优化 | 缓存计算结果（60秒更新一次），不每次请求都重算全部日志 | 1h |
| 15 | 实时比分改为并发拉取 | `fetch_scores.py` 用 `concurrent.futures.ThreadPoolExecutor` 并发拉取104场比赛 | 2h |
| 16 | /api/all-stats 并发查询 | 当前串行查询5个端口（8081-8085），每个超时2秒，最慢10秒。改用 `ThreadPoolExecutor` 并发查询，共享2秒总超时 | 1h |
| 17 | /api/lineup 加内存缓存 | 每次阵容请求都直接调用 `api.fifa.com`。10个用户看同一场比赛 = 10次外部API调用。内存缓存5分钟（键 = fifa_id，值 = lineup结果 + 缓存时间戳） | 1h |
| 18 | /api/video 加内存缓存 | 每次播放都调用2次外部API（videoPlayerData + preplay）。多人看同一场比赛 = 重复调用。缓存 playURL 结果5分钟（与 #17 同理） | 1h |

### 数据安全（按投入产出比排序）

| # | 建议 | 具体方案 | 工作量 |
|---|------|---------|--------|
| 19 | 同步前备份 .bak | `wc-scores.json` 等文件在每次同步时被覆盖，如果同步写出错，上一版数据就没了。建议同步前先拷贝一份 `.bak`。成本几乎为零 | 15min |
| 20 | JSON 并发写加锁 | write → temp文件 → rename（原子操作），或加 `fcntl/msvcrt` 文件锁 | 2h |
| 21 | fetch_scores.py 加重试 + 限速退避 | 对 FIFA API 请求加 `try/except` + 3次重试 + 5秒间隔；检测 HTTP 429 响应码，自动指数退避；只有成功才覆盖 `wc-scores.json` | 2h |
| 22 | JS 解析器脆弱 | 用 `js2xml` 或 `pyjsparser` 替换括号计数；或加 `try/except`，解析失败时记录错误而不崩溃 | 3h |
| 23 | FIFA API 数据格式无校验 | 加 schema 校验关键字段（`matches[].id`、`matches[].status`、`matches[].score` 等）；格式不对时写告警日志而不是写入错误数据 | 2h |

**P1 总计：约 27h**

---

## 🟡 P2 — 架构问题（建议本月处理）

| # | 建议 | 具体方案 | 工作量 |
|---|------|---------|--------|
| 24 | 同步频率动态调整 | 有 live 比赛进行中 → 每30-60秒同步；有今日未开赛 → 每5分钟同步；无比赛日 → 每30分钟同步（仅检测是否有新比赛开始）。检测逻辑：读取 `wc-scores.json` 的 `match_details`，看 `status` 字段 | 2h |
| 25 | 增加定时自动同步比分 | 用 `threading.Thread` 启动后台定时器，根据 #24 的动态频率调整间隔 | 1h |
| 26 | 增加实时比分推送 | 后端：进行中的比赛每30秒推送一次比分更新（SSE 或 WebSocket）；前端：有 live 比赛时每60秒自动 `/api/sync` | 6h |
| 27 | index.html 拆分 JS | 把3189行 JS 拆成 `js/timeline.js`、`js/video-cache.js`、`js/bracket.js`、`js/roster.js` 等；`js/` 目录已建好但未使用 | 8h |
| 28 | HLS.js 版本锁定 + 本地化 | 下载 `hls.js@1.5.7` 到 `js/hls.min.js`；修改 `index.html` 引用本地文件；删除 `@latest` | 1h |
| 29 | HLS.js 实例泄漏修复 | 切换比赛时如果旧 HLS 实例没有正确 `destroy()`，会持续占用内存和网络。手机端尤其明显，连续看几场后内存可能溢出。应在 `_playM3u8` 开头加 `if (hls) hls.destroy();` | 30min |
| 30 | 视频播放 URL 过期自动刷新 | server.py 获取 Uplynk `playURL` 后直接返回前端。该 URL 含时效性签名（通常15-30分钟过期），用户长时间暂停后再播放会失败。前端检测 HLS 播放失败时（`Hls.Events.ERROR` + `networkError`），自动调用 `/api/video?fifaId=xxx` 刷新 URL | 2h |
| 31 | 视频缓存 TTL 过期机制 | `_evictOldVideoCache` 目前只在配额超限时触发。小组赛结束后没人再看小组赛视频，缓存白白占空间。应加 TTL：缓存7天后自动标记为过期，下次访问时清理 | 2h |
| 32 | 前端全局错误捕获 | 当前没有任何 `window.onerror` / `window.addEventListener('error')` 处理。任何一个JS错误（比如数据解析失败）就可能导致页面白屏。加全局错误处理器，捕获后显示友好提示 + 发送到 `/api/client-error` | 2h |
| 33 | Service Worker 离线支持 | 目前服务器一挂全站不可用。加 Service Worker 可以缓存核心 HTML/CSS/JS，离线时仍能查看已加载过的积分榜和阵容。世界杯期间服务器重启/断网是常见场景 | 6h |
| 34 | 淘汰赛签表定义统一 | 把重复3处的签表定义提取到 `wc-bracket-config.json`；`fetch_scores.py`、`index.html`、JSON 都读这个文件 | 3h |
| 35 | 已完赛不用映射 | 已完赛的比赛不应再依赖映射。如果映射错了但比分已确定，应直接用比分条数据而非映射结果，避免传播错误 | 30min |
| 36 | 球员数据文件整合 | 合并 `wc-roster-data.js`、`wc-player-detail.js`、`wc-player-7m.js` 为单一数据源；或用脚本从单一来源生成这三个文件 | 4h |
| 37 | 黄牌停赛规则完整实现 | `wc-cards.json` 增加 `suspended` 字段；计算逻辑：小组赛累积2黄 → 下场停赛；小组赛结束后清零；1/4决赛后再次清零 | 4h |
| 38 | 视频发现依赖硬编码 Carousel ID | 提取到 `config.json`；增加发现失败时的告警（邮件/微信/Telegram） | 2h |
| 39 | 淘汰赛对阵映射容错 | 加映射结果缓存校验；FIFA API 格式变动时发告警；增加 fallback 逻辑（用上一场比赛的对阵） | 2h |
| 40 | 静态资源 Cache-Control | 对 `.png`、`.css` 加 `Cache-Control: max-age=3600`（1小时缓存）；对 `.js` 数据文件加 `max-age=300`（5分钟，兼顾数据更新）。版本更新时通过查询参数 `?v=xxx` 刷新。比 #49 ETag 更高效——零网络请求 | 30min |

**P2 总计：约 46h**

---

## 🟢 P3 — 项目卫生（建议有空就做）

| # | 建议 | 具体方案 | 工作量 |
|---|------|---------|--------|
| 41 | 清理根目录一次性文件 | 脚本移入 `scripts/`；文档移入 `docs/`；测试移入 `.temp/`；API抓取产物移入 `.temp/api-cache/` | 2h |
| 42 | 删除死代码 | 删除 `_log_apk_access` 和 `_log_page_view` 整个函数（不仅是开头的 `return`） | 30min |
| 43 | 日志文件移到 .temp/ 并做轮转 | `access.log`、`user_activity.log`、`apk_access.log` 移到 `.temp/logs/`；用 `logging.handlers.RotatingFileHandler` 限制大小（最大5MB） | 2h |
| 44 | 日志功能统一 | 要么全部启用（用 `logging` 模块，不同级别输出到不同文件）；要么全部禁用（当前半残状态容易误导） | 1h |
| 45 | 增加 /api/health 接口 | 返回：服务状态（up/down）、最后同步时间、数据文件状态（存在？大小？最后修改时间？）、磁盘剩余空间 | 1h |
| 46 | 服务器增加优雅停机 | `daemon_threads` 改为 `False`；`KeyboardInterrupt` 处理：调用 `server.shutdown()` 等待进行中的请求完成（30秒超时）；保存状态 | 2h |
| 47 | .gitignore 优化 | 区分"版本控制"（`.gitignore` 排除 `*.pyc`、`__pycache__/`）和"运行时数据"（不排除 `wc-photos/`、`wc-flags/`，但加到 `.git/info/exclude`） | 1h |
| 48 | 增加客户端错误监控 | `window.addEventListener('error', ...)` 捕获前端错误；发送到 `/api/client-error` 记录到日志 | 2h |
| 49 | 增加 ETag / 条件请求 | 对图片和静态 JS 文件添加 ETag（基于文件修改时间 + 大小），响应 `304 Not Modified` 时只返回头部。与 #40 互补：#40 消除首次往返，ETag 消除后续往返 | 2h |
| 50 | 优化 APK 构建流程 | `build_fifa.py` 加 `check_prerequisites()` 函数，构建前检查所有工具是否存在；工具列表写到 `requirements-build.txt` | 4h |
| 51 | build_fifa.py + server.py 路径硬编码改为相对路径 | `G:\AI\FIFA`、`G:\AI\APK`、`D:\迅雷下载` 等路径硬编码在多个文件里。改为 `SCRIPT_DIR` 相对路径 + 环境变量回退（`FIFA_APK_DIR`、`FIFA_VIDEO_DIR`） | 1h |
| 52 | 写文档 | `server.py` 的所有 API 端点写文档（可以用 swagger/OpenAPI 或简单 Markdown）；`build_fifa.py` 写使用说明 | 4h |
| 53 | 给核心逻辑加单元测试 | `fetch_scores.py` 加单元测试（mock FIFA API）；视频缓存加端到端测试（Puppeteer/Playwright） | 8h |
| 54 | 备份目录无限增长 | `G:\AI\backups\FIFA_*` 每次完整备份约220MB，没有清理机制。应保留最近3个备份，自动删除更早的 | 30min |
| 55 | 隧道URL自动同步 | `tunnel_url.txt` 存的是 Cloudflare tunnel URL，重启后URL会变。应加启动时自动更新机制，或者改用固定域名+DDNS（已有 `sethshi.dynv6.net`） | 1h |
| 56 | 增加 PWA manifest | 添加 `manifest.json` + `<link rel="manifest">`，移动浏览器可直接"添加到主屏幕"，不需要安装 APK。工作量极小 | 30min |
| 57 | error 响应无 CORS 头 | `send_error()` 调用时不会经过 `end_headers()` 的 CORS 逻辑，导致前端 `fetch` 捕获不到错误详情。应在 `send_error` 覆写中补上 CORS 头 | 15min |

**P3 总计：约 32.75h**

---

## 🎯 建议优先做的 5 件事（投入产出比最高）

| 优先级 | 事项 | 原因 | 工作量 |
|--------|------|------|--------|
| 1 | #1 /proxy/ + /video/ 加域名白名单 | 安全风险，必须立即修复 | 1h15min |
| 2 | #7 删除 wc-photos 重复目录 | 30分钟省550MB，投入产出比极高 | 30min |
| 3 | #8 HTTP 压缩（gzip） | `wc-player-7m.js` 4MB → 800KB，5个数据JS加起来压缩能省70%+流量 | 1h |
| 4 | #9 心跳机制改内存缓存 | 消除高频磁盘I/O（每30秒 2N 次 → 每5分钟1次） | 1h |
| 5 | #20 JSON 并发写加锁 | 数据损坏风险，可能导致视频注册信息丢失 | 2h |

**总计：约 6h45min，1-2 个工作日可以完成**

---

## 📋 具体修改代码示例

### #1 /proxy/ + /video/ 加域名白名单（server.py）

```python
ALLOWED_DOMAINS = ['webcurl.7m.com', 'api.fifa.com', 'uplynk.com']

def _is_allowed(url):
    return any(domain in url for domain in ALLOWED_DOMAINS)
```

在 `_handle_proxy` 和 `_handle_video`（远程URL分支）开头加：

```python
if not _is_allowed(url):
    self.send_response(403)
    self.send_header('Content-Type', 'text/plain')
    self.end_headers()
    self.wfile.write(b'Forbidden: domain not allowed')
    return
```

### #2 /video/ 限制文件扩展名（server.py）

在 `_handle_video` 本地文件分支加：

```python
ALLOWED_VIDEO_EXTS = {'.m3u8', '.ts', '.key', '.mp4'}

ext = os.path.splitext(fpath)[1].lower()
if ext not in ALLOWED_VIDEO_EXTS:
    self.send_error(403, 'File type not allowed')
    return
```

### #5 /api/sync 加 cooldown（server.py）

```python
_SYNC_COOLDOWN = 60  # seconds

def _handle_sync(self):
    now = time.time()
    last = getattr(self.server, '_last_sync_time', 0)
    if now - last < _SYNC_COOLDOWN:
        self._return_scores()
        return
    # ... 原有同步逻辑 ...
    # 同步完成后:
    self.server._last_sync_time = time.time()
```

### #8 HTTP 压缩 gzip（server.py）

```python
import gzip
import io

def end_headers(self):
    # 对 JS/JSON/HTML/CSS 自动 gzip
    if self._should_gzip():
        self.send_header('Content-Encoding', 'gzip')
    super().end_headers()

def _should_gzip(self):
    accept = self.headers.get('Accept-Encoding', '')
    if 'gzip' not in accept:
        return False
    path = self.path.split('?')[0]
    return any(path.endswith(ext) for ext in ('.js', '.json', '.html', '.css'))
```

### #20 JSON 并发写加锁（server.py）

```python
import tempfile

def _atomic_write_json(file_path, data):
    """原子写入JSON文件（写temp → rename）"""
    dir_name = os.path.dirname(file_path)
    fd, temp_path = tempfile.mkstemp(suffix='.tmp', dir=dir_name)
    try:
        with os.fdopen(fd, 'w', encoding='utf-8') as f:
            json.dump(data, f, ensure_ascii=False, indent=2)
        os.replace(temp_path, file_path)  # 原子操作
    except:
        os.unlink(temp_path)  # 失败时删除temp文件
        raise
```

### #9 心跳改内存缓存（server.py）

```python
# 在 main() 中初始化:
server._online_users = {}
server._online_users_last_flush = 0

def _handle_heartbeat(self):
    ip = self.client_address[0]
    # 只更新内存，不写文件
    self.server._online_users[ip] = {
        'ip': ip,
        'last_seen': timestamp,
        'page': page,
    }
    # 每5分钟才持久化一次
    now = time.time()
    if now - self.server._online_users_last_flush > 300:
        self.server._online_users_last_flush = now
        with open(online_file, 'w', encoding='utf-8') as f:
            json.dump(self.server._online_users, f, ensure_ascii=False, indent=2)
```

### #28 HLS.js 版本锁定 + 本地化

```bash
# 下载到本地
curl -o js/hls.min.js https://cdn.jsdelivr.net/npm/hls.js@1.5.7/dist/hls.min.js
```

```html
<!-- 改前 -->
<script src="https://cdn.jsdelivr.net/npm/hls.js@latest"></script>
<!-- 改后 -->
<script src="js/hls.min.js"></script>
```

---

## 📊 总工作量统计

| 优先级 | 条数 | 工作量 |
|--------|------|--------|
| P0 | 6条 | 约 4.75h |
| P1 | 17条 | 约 27h |
| P2 | 17条 | 约 46h |
| P3 | 17条 | 约 32.75h |
| **总计** | **57条** | **约 110.5h（约 14 个工作日）** |

---

## 📝 变更记录

| 版本 | 日期 | 变更 |
|------|------|------|
| v1 | 2026-06-15 | 初始版本，32条 |
| v2 | 2026-06-15 | 融合第2轮审查，扩展至42条，增加P0安全层级 |
| v3 | 2026-06-15 | 融合第3轮审查，扩展至53条，增加代码示例 |
| v4 | 2026-06-15 | 融合第4轮审查，增加缩略图、数据校验、HLS泄漏等 |
| v5 | 2026-06-15 | 终版，增加 /video/ 扩展名限制、/api/sync cooldown、/api/video 缓存、静态资源 Cache-Control、server.py 路径灵活化、daemon_threads 修复、PWA manifest、error CORS，共57条 |
| v5+ | 2026-06-15 | 增加3人并行实施计划 |

---

## 🏗️ 三人并行实施计划

### 人员分工原则

| 角色 | 定位 | 主攻方向 | 主要修改文件 |
|------|------|---------|-------------|
| **1#** 后端安全 | server.py 守护者 | 安全防护、数据完整性、后端基础能力 | server.py、fetch_scores.py |
| **2#** 性能基建 | 性能工程师 | 缓存、压缩、流式、并发、基础设施 | server.py、fetch_scores.py、build_fifa.py |
| **3#** 前端体验 | 前端工程师 | 前端性能、UX、架构拆分、视频播放 | index.html、JS/CSS、PWA |

> **关键约定**：1# 和 2# 都会改 server.py，必须按阶段错开——1# 先改安全相关代码，2# 再改性能相关代码，合并时解决冲突。

### 工作量分配总览

| 角色 | 分配条数 | 总工时 | 占比 |
|------|---------|--------|------|
| 1# 后端安全 | 19条 | ~35h | 32% |
| 2# 性能基建 | 19条 | ~37h | 33% |
| 3# 前端体验 | 19条 | ~38.5h | 35% |
| **合计** | **57条** | **~110.5h** | 100% |

---

### Sprint 1：P0 安全加固（Day 1-2，共 4.75h）

> 目标：消除所有安全隐患，确保上线安全

#### 1# 后端安全（Day 1 全天，4.75h）

| 顺序 | 条# | 任务 | 工时 | 验证方式 |
|------|-----|------|------|----------|
| 1 | #1 | /proxy/ + /video/ 域名白名单 | 1.25h | `curl http://localhost:8086/proxy/https://evil.com` → 403；合法域名正常代理 |
| 2 | #2 | /video/ 限制文件扩展名 | 0.25h | `curl http://localhost:8086/video/../../etc/passwd` → 403；`.txt` → 403；`.m3u8` → 正常 |
| 3 | #3 | /api/stats 加鉴权 | 1h | 无 Token 请求 → 401/简化数据；有 Token → 完整数据 |
| 4 | #4 | /api/diag 加限制 | 1h | 大于1MB body → 413；同一IP第6次/分钟 → 429 |
| 5 | #5 | /api/sync 加 cooldown | 0.25h | 60秒内第2次同步 → 返回缓存数据 |
| 6 | #6 | CORS 收紧 | 0.5h | 非 `192.168.0.10:8086` 来源 → 浏览器拦截跨域 |

**Sprint 1 交付物**：server.py 安全加固版本，所有 P0 项通过验证

#### 2# 性能基建（Day 1-2，与 1# 错开改 server.py）

| 顺序 | 条# | 任务 | 工时 | 验证方式 |
|------|-----|------|------|----------|
| 1 | #7 | 删除 wc-photos 重复目录 | 0.5h | 确认只剩48个短代码目录；页面照片正常加载 |
| 2 | #8 | HTTP 压缩 gzip | 1h | `curl -H "Accept-Encoding: gzip" -I http://localhost:8086/wc-player-7m.js` → `Content-Encoding: gzip`；响应体大小对比 |

> 2# 在 Day 1 先做不碰 server.py 的 #7；Day 2 等待 1# 完成后再改 server.py 做 #8

#### 3# 前端体验（Day 1-2，独立工作）

| 顺序 | 条# | 任务 | 工时 | 验证方式 |
|------|-----|------|------|----------|
| 1 | #28 | HLS.js 版本锁定 + 本地化 | 1h | 断网后 HLS.js 仍可加载；确认版本为 1.5.7 |
| 2 | #29 | HLS.js 实例泄漏修复 | 0.5h | Chrome DevTools → Network：切换比赛时旧 m3u8 请求停止；Memory 无持续增长 |
| 3 | #56 | PWA manifest | 0.5h | Chrome → "添加到主屏幕" 出现；manifest.json 校验通过 |

**Sprint 1 检查点（Day 2 结束）**：
- [ ] 所有 P0 安全测试通过（1# 主导）
- [ ] wc-photos 只剩短代码目录（2#）
- [ ] gzip 压缩生效（2#）
- [ ] HLS.js 本地化 + 无泄漏（3#）
- [ ] 三方代码合并，无冲突

---

### Sprint 2：P1 核心性能 + 数据安全（Day 3-5，共 ~24h）

> 目标：性能翻倍提升 + 数据不再丢失

#### 1# 后端安全（Day 3-4，6.25h）

| 顺序 | 条# | 任务 | 工时 | 依赖 | 验证方式 |
|------|-----|------|------|------|----------|
| 1 | #19 | 同步前备份 .bak | 0.25h | 无 | 手动同步后检查 `.bak` 文件存在且内容正确 |
| 2 | #20 | JSON 原子写 | 2h | 无 | 并发写入测试：10个线程同时写同一JSON，内容不损坏 |
| 3 | #21 | fetch_scores 重试+退避 | 2h | #20 | 模拟 FIFA API 429/500 → 自动重试+退避；最终失败不覆盖数据 |
| 4 | #23 | API 数据格式校验 | 2h | #21 | 传入畸形 JSON → 写告警日志但不写文件；正常数据正常写入 |

#### 2# 性能基建（Day 3-5，12.5h）

| 顺序 | 条# | 任务 | 工时 | 依赖 | 验证方式 |
|------|-----|------|------|------|----------|
| 1 | #9 | 心跳改内存缓存 | 1h | #20（用原子写） | 监控磁盘I/O：`Resource Monitor` 确认不再每30秒写文件 |
| 2 | #12 | 大文件流式传输 | 2h | 无 | `curl` 下载 APK → 逐步收到数据而非等待全量；内存占用稳定 |
| 3 | #13 | 代理流式传输 | 2h | 无 | 播放视频 → 内存不随时间增长；大视频不 OOM |
| 4 | #14 | Stats API 缓存 | 1h | 无 | 连续请求 → 第2次响应 <10ms；60秒后缓存刷新 |
| 5 | #16 | /api/all-stats 并发查询 | 1h | 无 | 5个端口全响应 → 总耗时 <2秒（原来最长10秒） |
| 6 | #17 | /api/lineup 缓存 | 1h | 无 | 10次同场阵容请求 → 只1次外部API调用 |
| 7 | #18 | /api/video 缓存 | 1h | #17 同模式 | 10次同场视频请求 → 只1次外部API调用 |
| 8 | #15 | 比分并发拉取 | 2h | #21（重试机制） | 同步104场：原来~30秒 → <5秒 |
| 9 | #22 | JS 解析器加固 | 1.5h | 无 | 畸形JS输入 → 异常被捕获+日志，不崩溃 |

#### 3# 前端体验（Day 3-5，10h）

| 顺序 | 条# | 任务 | 工时 | 依赖 | 验证方式 |
|------|-----|------|------|------|----------|
| 1 | #11 | 照片缩略图预生成 | 3h | 无 | Python脚本批量生成 `wc-photos-thumb/`；单张 <20KB；页面加载 <2MB |
| 2 | #10 | wc-player-7m.js 按需加载 | 4h | 无 | 首次加载不请求此文件；点击球员 → 动态加载；Network 面板确认 |
| 3 | #30 | 视频 URL 过期自动刷新 | 2h | 2# #18 | 暂停视频30分钟 → 恢复播放自动刷新URL，不报错 |
| 4 | #35 | 已完赛不用映射 | 1h | 无 | 手动改映射为错误值 → 已完赛比赛仍显示正确比分 |

**Sprint 2 检查点（Day 5 结束）**：
- [ ] 所有 P1 数据安全项通过（1# 主导）
- [ ] 性能基准测试：页面加载时间降低50%+（2# 主导）
- [ ] 缩略图 + 按需加载生效（3#）
- [ ] 三方代码合并，回归测试通过

---

### Sprint 3：P2 架构核心（Day 6-10，共 ~46h）

> 目标：架构可维护、可扩展、可离线

#### 1# 后端安全（Day 6-8，10h）

| 顺序 | 条# | 任务 | 工时 | 依赖 | 验证方式 |
|------|-----|------|------|------|----------|
| 1 | #24 | 同步频率动态调整 | 2h | #15 | 有live → 30秒同步；无live → 5分钟；无比赛 → 30分钟 |
| 2 | #25 | 定时自动同步 | 1h | #24 | 启动服务器 → 自动开始同步循环，无需手动触发 |
| 3 | #34 | 签表定义统一 | 3h | 无 | 3处签表代码删除，只读 `wc-bracket-config.json`；淘汰赛显示正确 |
| 4 | #38 | Carousel ID 配置化 | 2h | 无 | Carousel ID 从 `config.json` 读取；修改配置 → 新ID生效 |
| 5 | #39 | 映射容错 | 2h | #34 | API 格式变化 → 日志告警 + fallback 到上次映射 |

#### 2# 性能基建（Day 6-9，9h）

| 顺序 | 条# | 任务 | 工时 | 依赖 | 验证方式 |
|------|-----|------|------|------|----------|
| 1 | #36 | 球员数据文件整合 | 4h | 3# #10 | 3个JS文件由1个脚本生成；数据一致性校验通过 |
| 2 | #40 | 静态资源 Cache-Control | 0.5h | #8 | `curl -I wc-flags/ARG.png` → `Cache-Control: max-age=3600` |
| 3 | #49 | ETag / 条件请求 | 2h | #40 | 第2次请求同一图片 → `304 Not Modified`；无响应体 |
| 4 | #51 | 路径硬编码改为相对路径 | 1h | 无 | 删除环境变量 → 默认路径可用；设置环境变量 → 覆盖生效 |
| 5 | #54 | 备份目录清理 | 0.5h | 无 | 运行清理脚本 → 只保留最近3个备份 |
| 6 | #55 | 隧道URL自动同步 | 1h | 无 | 重启 Cloudflare tunnel → 新URL自动写入 `tunnel_url.txt` |

#### 3# 前端体验（Day 6-10，22.5h）

| 顺序 | 条# | 任务 | 工时 | 依赖 | 验证方式 |
|------|-----|------|------|------|----------|
| 1 | #31 | 视频缓存 TTL 过期 | 2h | 无 | 7天后访问旧缓存 → 自动清理；新视频正常缓存 |
| 2 | #27 | index.html JS 拆分 | 8h | #10 | 所有功能正常；Network 面板：按需加载各JS模块 |
| 3 | #32 | 前端全局错误捕获 | 2h | #27 | 手动触发 JS 错误 → 显示友好提示 + `/api/client-error` 收到报告 |
| 4 | #37 | 黄牌停赛规则 | 4h | 无 | 模拟2黄 → 下场显示停赛标记；小组赛结束 → 清零 |
| 5 | #26 | 实时比分推送 | 6h | 1# #25 | 打开比分页 → 比分自动更新，无需刷新；延迟 <60秒 |

**Sprint 3 检查点（Day 10 结束）**：
- [ ] 动态同步 + 自动同步生效（1#）
- [ ] 签表配置化 + 映射容错（1#）
- [ ] 缓存 + ETag + 路径灵活化（2#）
- [ ] JS 拆分 + 实时推送（3#）
- [ ] 全量功能回归测试
- [ ] 移动端回归测试（手机实际访问）

---

### Sprint 4：P2 收尾 + P3 项目卫生（Day 11-14，共 ~35h）

> 目标：收尾剩余架构项 + 项目规范化

#### 1# 后端安全（Day 11-13，9.75h）

| 顺序 | 条# | 任务 | 工时 | 依赖 | 验证方式 |
|------|-----|------|------|------|----------|
| 1 | #41 | 清理根目录 | 2h | 无 | 根目录只剩核心文件；`scripts/`、`docs/` 就位 |
| 2 | #42 | 删除死代码 | 0.5h | 无 | 全文搜索确认无残留引用 |
| 3 | #43 | 日志移到 .temp/ + 轮转 | 2h | 无 | 日志文件 >5MB → 自动轮转；旧日志自动清理 |
| 4 | #44 | 日志功能统一 | 1h | #43 | `logging` 模块统一管理；不同级别输出到不同文件 |
| 5 | #45 | /api/health 接口 | 1h | 无 | `curl /api/health` → 返回服务状态、同步时间、磁盘空间 |
| 6 | #46 | 优雅停机 | 2h | 无 | Ctrl+C → 日志显示"等待请求完成" → 正常退出；无半写文件 |
| 7 | #57 | error 响应 CORS 头 | 0.25h | 无 | `fetch` 错误请求 → 前端能读取错误信息 |

#### 2# 性能基建（Day 11-13，15h）

| 顺序 | 条# | 任务 | 工时 | 依赖 | 验证方式 |
|------|-----|------|------|------|----------|
| 1 | #50 | APK 构建流程优化 | 4h | #51 | 缺少工具 → 构建前报错；`requirements-build.txt` 完整 |
| 2 | #53 | 核心逻辑单元测试 | 8h | #21, #22, #23 | `pytest` → 全部通过；覆盖重试、解析、校验逻辑 |
| 3 | #47 | .gitignore 优化 | 1h | #41 | `git status` → 运行时资源不再被排除；`.git/info/exclude` 正确配置 |
| 4 | #52 | API 文档 | 2h | #45 | 所有 `/api/*` 端点有文档；请求/响应示例完整 |

#### 3# 前端体验（Day 11-14，6.5h）

| 顺序 | 条# | 任务 | 工时 | 依赖 | 验证方式 |
|------|-----|------|------|------|----------|
| 1 | #33 | Service Worker 离线支持 | 6h | #27 | 断开服务器 → 已访问页面仍可正常显示；缓存命中率 >80% |
| 2 | #48 | 客户端错误监控 | 2h | #32 | 前端 JS 错误 → `/api/client-error` 收到完整错误栈 |

> **注意**：#33 Service Worker 工作量大，如果时间不足可推迟到下一迭代。

**Sprint 4 检查点（Day 14 结束）**：
- [ ] 项目目录结构规范（1#）
- [ ] 日志 + 健康检查 + 优雅停机（1#）
- [ ] 单元测试 + APK构建 + 文档（2#）
- [ ] Service Worker 离线可用（3#）
- [ ] **最终全量回归测试**
- [ ] **上线前安全扫描**

---

### 📅 甘特图总览

```
Day   1# 后端安全              2# 性能基建              3# 前端体验
───── ────────────────────── ────────────────────── ──────────────────────
D1    #1 域名白名单            #7 删重复目录            #28 HLS.js本地化
      #2 扩展名限制                                    #29 HLS泄漏修复
      #3 stats鉴权                                     #56 PWA manifest
      #4 diag限制
      #5 sync cooldown
      #6 CORS收紧
───── ────────────────────── ────────────────────── ──────────────────────
D2    P0验证+修bug             #8 gzip压缩             补充验证
      合并1#代码               合并2#代码               合并3#代码
───── ────────────────────── ────────────────────── ──────────────────────
D3    #19 备份.bak             #9 心跳改内存            #11 缩略图脚本
      #20 JSON原子写           #12 大文件流式           #10 按需加载
      #21 重试+退避            #13 代理流式
───── ────────────────────── ────────────────────── ──────────────────────
D4    #23 API数据校验          #14 Stats缓存            #10 按需加载(续)
                               #16 all-stats并发        #30 URL过期刷新
                               #17 lineup缓存           #35 已完赛不映射
                               #18 video缓存
───── ────────────────────── ────────────────────── ──────────────────────
D5    P1数据安全验证           #15 并发拉取             P1前端验证
      合并代码                 #22 JS解析器             合并代码
                               合并代码
───── ────────────────────── ────────────────────── ──────────────────────
D6    #24 动态同步频率         #36 球员数据整合         #31 视频缓存TTL
      #25 定时自动同步                                 #27 JS拆分(开始)
───── ────────────────────── ────────────────────── ──────────────────────
D7    #34 签表统一             #36 球员数据整合(续)     #27 JS拆分(续)
───── ────────────────────── ────────────────────── ──────────────────────
D8    #38 Carousel ID配置      #40 Cache-Control        #27 JS拆分(续)
      #39 映射容错             #49 ETag                #32 全局错误捕获
───── ────────────────────── ────────────────────── ──────────────────────
D9    P2后端验证               #51 路径灵活化           #37 黄牌停赛
      合并代码                 #54 备份清理
                               #55 隧道URL
───── ────────────────────── ────────────────────── ──────────────────────
D10   集成测试                 P2基建验证               #26 实时推送
                               合并代码                 合并代码
───── ────────────────────── ────────────────────── ──────────────────────
D11   #41 目录清理             #50 APK构建优化          #33 Service Worker
      #42 死代码删除           #53 单元测试(开始)
      #43 日志轮转
───── ────────────────────── ────────────────────── ──────────────────────
D12   #44 日志统一             #53 单元测试(续)         #33 Service Worker(续)
      #45 /api/health
      #46 优雅停机
      #57 error CORS
───── ────────────────────── ────────────────────── ──────────────────────
D13   P3后端验证               #47 gitignore优化        #48 客户端错误监控
      合并代码                 #52 API文档
                               合并代码
───── ────────────────────── ────────────────────── ──────────────────────
D14   ═══════════════ 最终集成测试 + 上线 ═══════════════
───── ────────────────────── ────────────────────── ──────────────────────
```

---

### 🔗 跨人依赖关系

```
1# #20 JSON原子写 ──────→ 2# #9 心跳改内存（用原子写模式）
1# #21 重试+退避  ──────→ 2# #15 并发拉取（并发需要重试保障）
1# #24 动态同步    ──────→ 1# #25 定时自动同步（25依赖24的频率逻辑）
1# #34 签表统一    ──────→ 1# #39 映射容错（39依赖统一后的配置）
2# #18 video缓存   ──────→ 3# #30 URL过期刷新（30依赖缓存API接口）
2# #8 gzip压缩     ──────→ 2# #40 Cache-Control（同属HTTP头，顺序做）
2# #40 Cache-Control ───→ 2# #49 ETag（49依赖40的头部处理框架）
3# #27 JS拆分      ──────→ 2# #36 球员数据整合（整合后文件结构变化）
3# #10 按需加载    ──────→ 3# #27 JS拆分（10改变加载方式，27进一步拆分）
3# #32 全局错误    ──────→ 3# #48 客户端监控（48依赖32的捕获逻辑）
1# #25 定时同步    ──────→ 3# #26 实时推送（26依赖25的同步基础设施）
```

---

### ✅ 验证清单

每个 Sprint 结束后，三方共同执行以下验证：

| 类别 | 验证项 | 方法 | 通过标准 |
|------|--------|------|----------|
| 安全 | 代理不可滥用 | `curl /proxy/https://evil.com` | 403 |
| 安全 | 文件不可遍历 | `curl /video/../../etc/passwd` | 403 |
| 安全 | API 限流生效 | 连续快速请求 | 429 |
| 性能 | gzip 压缩 | `curl -H "Accept-Encoding: gzip" -I` | Content-Encoding: gzip |
| 性能 | 页面加载时间 | Chrome Lighthouse | <3秒（原来 >8秒） |
| 性能 | 内存稳定 | 播放视频30分钟 | 内存不超 500MB |
| 功能 | 比分同步 | 手动触发 + 自动触发 | 数据正确，无报错 |
| 功能 | 视频播放 | 播放3场不同比赛 | 正常播放，无泄漏 |
| 功能 | 离线访问 | 断开服务器后刷新 | 已缓存页面正常 |
| 数据 | JSON 完整性 | 并发写入测试 | 无损坏 |
| 兼容 | 移动端 | 手机实际访问 | 功能正常 |
| 兼容 | APK | 安装 + 打开 | 正常连接服务器 |
